# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sfa_database
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SSL=true
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# MSSQL Database Configuration (for User Hierarchy)
MSSQL_SERVER=10.20.53.178
MSSQL_PORT=1433
MSSQL_DATABASE=FarmleyQA
MSSQL_USER=farmleyqa
MSSQL_PASSWORD=Winit%123$

# Application Environment
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Google Maps API Key (MUST use NEXT_PUBLIC_ prefix for client-side access)
# WARNING: This key will be visible in browser - enable domain restrictions in Google Cloud Console
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Security Configuration
# Comma-separated list of allowed referrer domains (e.g., https://mainsite.com,https://www.mainsite.com)
# For development, use http://localhost:3000 or your main site's development URL
# Add 'file://' to test with local HTML files
ALLOWED_REFERRERS=file://,http://localhost:3000,http://localhost:3001
NEXT_PUBLIC_ALLOWED_REFERRER=http://localhost:3000